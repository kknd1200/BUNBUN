name: Verify BUNBUN Vercel runtime

on:
  workflow_dispatch:

permissions:
  contents: read
  deployments: read
  statuses: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: echo "Manual verification only"
